part of '_themes.dart';

class BaseColors {
  // Warm terracotta — food/appetite-coded, distinct from the muted greens
  // of the project this scaffold was originally built for.
  static const Color primary = Color(0xFFE4572E);
  static const Color primaryText = Color(0xFFE4572E);

  static const Color black = Colors.black;
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;

  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFDC2626);
}
